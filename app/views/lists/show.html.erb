<h1 class="text-center"><%= @list.name %></h1>

<p class="text-right"><%= link_to "Add a Bookmark", new_list_bookmark_path(@list) %></p>
<div class="cards">
<div class="card-trip">
  <div class="card-trip-infos">
<% @list.bookmarks.each do |bookmark| %>
<div class="card-category" style="background-image: url('<%= asset_path(bookmark.movie.poster_url) %>');">

  <%# <img src="..." class="card-img-top" alt="..."> %>
  <div class="card-body">
    <h5 class="card-title"><%= bookmark.movie.title %></h5>


      <h6><%= bookmark.comment %></h6>
      <%= link_to "Delete bookmark", bookmark_path(bookmark), data: {turbo_method: 'delete', turbo_confirm: "delete bookmark?"} %>


    <%# <div class="card text-end">
    <p class="card-text"><%= bookmark.comment %>
    <%# <%= link_to "Delete bookmark", bookmark_path(bookmark), data: {turbo_method: 'delete', turbo_confirm: "delete bookmark?"} %>


    <%# <a href="#" class="btn btn-primary">Go somewhere</a> %>
  </div>
</div>
  </div>
</div>
<% end %>
</div>
<p><%= link_to "Lists", lists_path, class: "btn btn-primary" %></p>
